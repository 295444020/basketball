# 篮球比赛计分系统 · 设计说明

> 交付物：`index.html`（计分台，单文件网页应用，双击即可运行，离线可用，零依赖）与 `display.html`（大屏显示页，配套的独立网页）

## 1. 设计目标

以 **FIBA 国际篮联标准规则**为默认依据，做一款现场可用的篮球计分软件：

- 记录员一人在一台电脑上即可完成全部操作（记分、计时、犯规、暂停、统计）；
- 所有规则参数和球队名单均由管理员在界面内自定义，不写死；
- 单文件、免安装、自动保存，适配场馆大屏投影。

## 2. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 形态 | 单文件 HTML + 原生 JS | 免安装、离线、任何浏览器可打开 |
| 数据存储 | `localStorage` 自动保存 + JSON 导入导出 | 断电/误关可恢复，可跨设备迁移 |
| 计时 | `performance.now()` 差值累加 | 不受定时器漂移影响，精确到 0.1 秒 |
| 提示音 | Web Audio API 振荡器 | 无需音频文件即可发出蜂鸣/终场哨 |

## 3. 模块架构（11 个业务模块 + 1 个规则引擎）

```
规则引擎（管理员自定义：赛制预设 / 规则参数 / 名单管理）
        │
 ┌──────┴──────────────┬─────────────────────┬──────────────────────┐
 │ ① 比赛控制           │ ② 现场记录           │ ③ 数据服务            │
 │  · 比赛时钟模块      │  · 比分模块(+1/+2/+3) │  · 球员与名单模块      │
 │  · 进攻时限模块(24″) │  · 犯规模块(BONUS)    │  · 技术统计模块        │
 │  · 节次与加时模块    │  · 暂停模块          │  · 比赛记录(日志)模块  │
 │                     │  · 球权指示          │  · 本地存储模块        │
 └─────────────────────┴─────────────────────┴──────────────────────┘
        │
   显示与数据出口：独立大屏显示页 · 撤销栈 · 导出 JSON / CSV
```

各模块职责：

1. **比赛时钟模块** — 节次倒计时、开始/暂停（空格键）、节末自动停表并蜂鸣。
2. **进攻时限模块** — 24 秒（3x3 为 12 秒）倒计时；低于 5 秒变红；耗尽自动判罚违例、停表、换球权；支持一键重置 24″/14″（前场篮板 14 秒规则）。
3. **节次与加时模块** — 按设定的节数自动推进，加时赛自动切换时长，节间全队犯规清零。
4. **比分模块** — +1（罚球）/+2/+3；可先点选球员再记分，得分同时写入该球员技术统计；支持 −1/−2 更正与一键撤销。
5. **犯规模块** — 个人犯规计数，达到上限自动标记「罚下」并禁止重新上场；全队犯规按节累计，达到阈值自动亮起 **BONUS 处罚**标记。
6. **暂停模块** — 上/下半场与加时分别限次（如 FIBA 上半场 2 次、下半场 3 次），超限拦截；请求暂停自动停表。
7. **球员与名单模块** — 号码、姓名、位置、在场状态全部可增删改；在场/替补/罚下三态可视化。
8. **技术统计模块** — 2 分、3 分、罚球、篮板、助攻、抢断、盖帽、失误、犯规逐项 ±修正；得分由 2×2分 + 3×3分 + 罚球自动汇总；支持导出 CSV（Excel 可直接打开）。
9. **比赛记录模块** — 带「第 X 节 · 剩余时间」的逐回合日志，所有得分/犯规/暂停/违例/换人自动落账。
10. **规则引擎（设置页）** — 所有模块的上层配置中心，见下节。
11. **大屏显示页（`display.html`，独立网页）** — 计分台点「打开大屏显示」弹出的独立窗口，采用**场馆 LED 记分牌风格**：黑底屏面 + 发光数字，顶部红色赛事标题，其下黄字队名「A VS B + 第 X 节」；左右两侧为双方球员名单（号码、姓名、犯规指示点、得分，在场球员排前、替补淡显、罚下标红删除线）；中央为琥珀色超大比分、红色比赛时钟（精确到 0.1 秒）、进攻时限与暂停指示块；底栏为双方累计犯规、BONUS 处罚徽标与球权指示。顶部工具条平时半透明、悬停才显现，不干扰观赛。

### 大屏显示页与计分台的同步机制

- 计分台把一份轻量快照（比分/时钟/犯规/暂停/**双方球员名单**等）通过 `BroadcastChannel` + `localStorage` 镜像双通道广播，约 8 次/秒；
- 大屏页三路接收：BroadcastChannel → `storage` 事件 → 250ms 轮询兜底，任一通道可用即可工作；
- 两次更新之间大屏页用本地时钟自行推算倒计时，保证秒表平滑走动、不闪烁；
- 断开时顶部状态灯转为「等待计分台数据」，恢复后自动续上；
- 大屏页自身可调：`F` 全屏、`D` 深浅色、`+`/`-` 字号缩放、`P` 演示模式，偏好自动记忆；演示模式内置一套完整假数据（含球员名单），便于投放前彩排。
- 大屏顶部的赛事标题可在计分台「规则与球队设置 → 大屏赛事标题」中自定义（例：2025-2026赛季城市篮球联赛）。
12. **登录与账号模块** — 操作台口令保护：打开页面先进入登录门，必须输入用户名和密码才能使用系统。账号分**管理员 / 操作员**两种角色；首次使用自动生成默认管理员 `admin / admin123`，登录后请立即修改。密码以**加盐 SHA-256 散列**存储在本机（从不保存明文，比赛数据导出文件中也不含账号信息）。会话保存在 `sessionStorage`，刷新页面不掉线，关闭浏览器后自动登出。账号管理入口在「规则与球队设置 → 登录账号管理」：管理员可添加账号、删除账号（不可删除自己、至少保留一个账号）、免原密码重置任意账号的密码；操作员只能修改自己的密码（需验证原密码）。登录门激活期间锁定整个页面（含空格键计等快捷键），防止未授权操作。

## 4. 规则引擎：管理员可自定义项

**赛制预设**（一键切换，切完仍可微调）

| 预设 | 节数×时长 | 进攻时限 | 个人犯规离场 | 暂停 |
|---|---|---|---|---|
| FIBA 国际篮联标准 | 4 × 10 分钟 | 24 秒 | 5 次 | 上半场 2 / 下半场 3 / 加时 1，每次 60 秒 |
| NBA 职业联赛标准 | 4 × 12 分钟 | 24 秒 | 6 次 | 上半场 4 / 下半场 3 / 加时 2，每次 75 秒 |
| FIBA 三人篮球 3x3 | 1 × 10 分钟（21 分制） | 12 秒 | 5 次 | 1 次，每次 30 秒 |
| 自定义 | 完全自定义 | 自定义 | 自定义 | 自定义 |

**逐项参数**：比赛节数、每节时长、加时时长、进攻时限秒数、暂停时长、个人犯规离场次数、全队犯规处罚阈值（BONUS）、各节段暂停次数、处罚状态罚球次数；以及三个开关：是否启用进攻时限、是否启用全队犯规处罚、是否启用犯满离场。

**其他**：两队队名、完整球员名单、球权交换；数据层支持导出/导入 JSON、导出统计 CSV、重新开始本场比赛、恢复出厂设置。

## 5. 数据模型

```
S = {
  rules:  { periods, periodMinutes, overtimeMinutes, shotClock,
            personalFoulLimit, teamFoulLimit, timeouts..., 开关... },
  home / away: { name, players: [{ id, num, name, pos, onCourt,
            fgm2, fgm3, ftm, reb, ast, stl, blk, tov, pf, out }] },
  match:  { period, clockMs, shotMs, running 状态,
            score: {home, away},
            periodScore: [ 每节比分 ],
            teamFouls:   [ 每节全队犯规 ],
            timeouts:    { h1, h2, ot } 每队已用次数,
            possession, selected, log: [] }
}
```

每次写操作前先压入快照栈（最多 80 步），`Ctrl+Z` / 「撤销上一步」即可回滚任意误操作。

## 6. 内置标准规则要点（应用内「规则说明」页同步展示）

- 得分：三分线外 3 分、线内 2 分、罚球 1 分；
- 进攻时限 24 秒（前场获球且不足 14 秒时重置 14 秒）、8 秒过半场、3 秒区、5 秒发球；
- 个人犯规累计 5 次罚出场外；单节全队第 5 次犯规起进入处罚状态，此后非投篮犯规对方获 2 次罚球；
- 暂停仅能由持球方或死球时请求，上半场 2 次、下半场 3 次、加时 1 次、每次 60 秒；
- 常规时间打平进入 5 分钟加时，直至分出胜负。

## 7. 后续扩展方向

- 多端同步：把 `match` 状态通过 WebSocket 推送到观众端小程序 / 大屏；
- 视频回放标记：在日志模块上叠加时间戳，联动录像回放；
- 阵容时间统计：按 `onCourt` 变化记录每位球员上场时长；
- 联赛化：JSON 导入导出已具备，可平滑接入赛程表与积分榜。
